# Copy to .env.deploy on your Mac (that file is gitignored — never pushed to GitHub).
# Use ~/apps/MouseXGene for REMOTE_DIR (do not expand to a local Mac path).
SERVER=ubuntu@YOUR_SERVER
REMOTE_DIR=~/apps/MouseXGene
